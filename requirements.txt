# Full export/build environment (NOT the pip-install deps -- see pyproject.toml).
# executorch is built from source for the MLX/CoreML delegates; not installable from PyPI.
torch>=2.12.0
cached_conv>=2.5.0
executorch==1.3.1
coremltools>=9.0